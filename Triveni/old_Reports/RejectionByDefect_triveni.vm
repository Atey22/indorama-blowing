{     "name" : "Rejection - by Defect",     "cols" : [{"name" : "number", "type" : "number", "description" : "Sr. No"},         {"name" : "defect", "type" : "string", "description" : "Defect"},           {"name" : "defectcode", "type" : "string", "description" : "Defect Code"},          {"name" : "machinename", "type" : "string", "description" : "Machine"},           
##{"name" : "wastagereason", "type" : "string", "description" : "Wastage Reason"},     
{"name" : "totalrejection", "type" : "long", "description" : "Rejection"},   {"name" : "remark", "type" : "string", "description" : "Remark"}   ],      #set($count = 0)      #set($number = 0)      "reportData" : [       #set($rejection = $resultmap.rejection)       #set($machine = $resultmap.machine)               #foreach($r in $rejection)       #foreach($rec in $machine)               #if($rec.get("machinename") == $r.get("_id").machinename)                 #if($count > 0)        ,             #end             {             #set($number = $count + 1)             "number" : $number,         "machinename": $r.get("_id").machinename,                        #if(!$r.get("_id").wastagereason)                  "wastagereason" : "-",                #else                  "wastagereason" : $r.get("_id").wastagereason,                #end                            #if(!$r.get("_id").defectcode)                 "defectcode" : "-",               #else        "defectcode" : $r.get("_id").defectcode,               #end         #if(!$r.get("_id").remark)                 "remark" : "-",               #else        "remark" : $r.get("_id").remark,               #end                #if(!$r.get("_id").defect)                 "defect" : "-",               #else        "defect" : $r.get("_id").defect,               #end                      "totalrejection" : $math.round($r.get("totalrejection"))        }       #set($count = $count + 1)            #end             #end              #end          ],   "records" : $count  }